# =============================================================================
# 后端本地运行覆盖项（由 backend-env.sh 迁移而来）。
# 与仓库根目录 .env 分类风格一致；仅包含 FastAPI / Celery 进程直接读取的变量。
# 复制为 backend/.env 后按需填写；本文件勿写入真实密钥或内网地址。
# =============================================================================

# -----------------------------------------------------------------------------
# 数据库
# -----------------------------------------------------------------------------

DATABASE_URL=mysql+aiomysql://username:password@127.0.0.1:3306/database_name

# -----------------------------------------------------------------------------
# 向量库（Milvus）
# -----------------------------------------------------------------------------

MILVUS_URI=http://127.0.0.1:19530

# -----------------------------------------------------------------------------
# Redis / Celery / 缓存 / 限流
# -----------------------------------------------------------------------------

REDIS_URL=redis://127.0.0.1:6379/0
CELERY_BROKER_URL=redis://127.0.0.1:6379/1
CELERY_RESULT_BACKEND_URL=redis://127.0.0.1:6379/2
CACHE_REDIS_URL=redis://127.0.0.1:6379/3
TASK_EVENT_REDIS_URL=redis://127.0.0.1:6379/4
GENERATION_RATE_LIMIT_REDIS_URL=redis://127.0.0.1:6379/5

# -----------------------------------------------------------------------------
# 媒体下载（yt-dlp，可选）
# -----------------------------------------------------------------------------

YTDLP_COOKIES_FILE=/path/to/cookies.txt

# -----------------------------------------------------------------------------
# Deep Research（DeerFlow 统一入口；见 backend/docs/DEEP_RESEARCH_DEERFLOW.md）
# -----------------------------------------------------------------------------

DEER_FLOW_BASE_URL=http://127.0.0.1:2026

# -----------------------------------------------------------------------------
# Deep Searcher（远程 HTTP：/upload、/load-files/、/query/）
# 可选；不设则使用 config.yaml 中 deep_searcher.deep_searcher_base_url
# -----------------------------------------------------------------------------

DEEP_SEARCHER_BASE_URL=http://127.0.0.1:8000
