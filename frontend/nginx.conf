server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Docker overlay / 虚拟化环境下 sendfile 可能导致大文件传输被截断（Firefox: NS_ERROR_NET_PARTIAL_TRANSFER）
    sendfile off;
    tcp_nopush off;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
